#!/usr/bin/env node

var currentPath = __dirname.replace(/\/bin$/, "");
var nodePath    = currentPath + "/node_modules";
var exec        = require('child_process').exec;
var argv        = process.argv.slice(2);
var argType     = argv[0];
