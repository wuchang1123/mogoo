(function(win){
  var Velocity = function(asts){
    this.asts = asts;
    this.init();
  };
  Velocity.Helper = {};
  Velocity.prototype = {
    constructor: Velocity
  };
  
  var guid = 0;
  //api map
  var utils = {
    forEach : _.each,
    some    : _.some,
    mixin   : _.mixin,
    guid    : function() {
    	guid++;
	    return "velocity-1111-2222" + guid;
    },
    isArray : _.isArray,
    indexOf : _.indexOf,
    // 1.2没有keys方法，考虑独立utils
    keys    : _.keys,
    now     : jQuery.now
  };

  {helper}
  {velocity}
  win.Velocity = Velocity;
})(this);
